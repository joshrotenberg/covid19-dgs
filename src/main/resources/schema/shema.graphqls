type Query {
    counties(countyFilter: String, stateFilter: String, fipsFilter: String): [County]
}

type County {
    date: String
    county: String
    state: String
    fips: String
    cases: Int
    deaths: Int
}